# ==============================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# ==============================================
# PENTING: Copy file ini menjadi .env.production dan isi semua nilai!
# Jangan commit file .env.production ke Git!

# ==============================================
# APPLICATION
# ==============================================
NODE_ENV=production
PORT=3000

# Timezone untuk cron jobs (Asia/Jakarta = WIB/UTC+7)
TZ=Asia/Jakarta

# ==============================================
# DATABASE
# ==============================================
# Format: postgresql://[USERNAME]:[PASSWORD]@[HOST]:[PORT]/[DATABASE_NAME]
# Contoh: postgresql://postgres:password123@localhost:5432/tiktok_hubs
DATABASE_URL=postgresql://[ISI_USERNAME]:[ISI_PASSWORD]@[ISI_HOST]:5432/tiktok_hubs

# ==============================================
# TIKTOK API
# ==============================================
# Dapatkan dari TikTok Developer Console
# Lihat DEPLOYMENT.md untuk panduan lengkap
TIKTOK_CLIENT_KEY=your_client_key_here
TIKTOK_CLIENT_SECRET=your_client_secret_here

# Redirect URI untuk OAuth callback
# HARUS sama dengan yang terdaftar di TikTok Developer Console
TIKTOK_REDIRECT_URI=https://api-tiktokhubs.humahub.my.id/auth/tiktok/callback

# ==============================================
# CORS
# ==============================================
# Daftar origin yang diizinkan (pisahkan dengan koma)
# PENTING: Tambahkan domain frontend dan backend Anda
CORS_ORIGIN=https://tiktokhubs.humahub.my.id,https://api-tiktokhubs.humahub.my.id

# ==============================================
# SECURITY KEYS
# ==============================================
# PENTING: Generate menggunakan scripts/generate-keys.sh
# JANGAN gunakan nilai default/contoh di production!

# Key untuk enkripsi token TikTok (64 karakter hex)
# Generate: openssl rand -hex 32
TOKEN_ENC_KEY=GENERATE_WITH_SCRIPT

# Secret untuk JWT token (base64, minimal 32 karakter)
# Generate: openssl rand -base64 32
JWT_SECRET=GENERATE_WITH_SCRIPT

# Secret untuk JWT refresh token (base64, minimal 32 karakter)
# Generate: openssl rand -base64 32
REFRESH_TOKEN_SECRET=GENERATE_WITH_SCRIPT

# Key untuk enkripsi data sensitif (64 karakter hex)
# Generate: openssl rand -hex 32
ENCRYPTION_KEY=GENERATE_WITH_SCRIPT

# ==============================================
# JWT CONFIGURATION
# ==============================================
# Durasi token dalam detik
JWT_EXPIRES_IN=900           # 15 menit
REFRESH_TOKEN_EXPIRES_IN=604800  # 7 hari

# ==============================================
# ADMIN API KEY (OPTIONAL)
# ==============================================
# API Key untuk akses admin langsung (bypass JWT)
# OPSIONAL: Kosongkan jika tidak digunakan
# Generate: openssl rand -base64 32
ADMIN_API_KEY=

# ==============================================
# RATE LIMITING
# ==============================================
# Maksimum request per window
RATE_LIMIT_MAX=100
# Window dalam detik
RATE_LIMIT_WINDOW_SECONDS=60

# ==============================================
# SYNC CONFIGURATION
# ==============================================
# Cron schedule untuk sync otomatis
# Default: "0 */6 * * *" = setiap 6 jam
# Format: minute hour day month weekday
SYNC_CRON_SCHEDULE=0 */6 * * *

# Jumlah hari data yang akan di-sync
# Default: 7 hari
SYNC_DAYS=7

# ==============================================
# LOGGING
# ==============================================
# Level: error, warn, info, debug
LOG_LEVEL=info

# ==============================================
# CATATAN
# ==============================================
# 1. Setelah mengisi semua nilai, rename file ini menjadi .env.production
# 2. Pastikan .env.production ada di .gitignore
# 3. Backup file .env.production di tempat aman
# 4. Untuk generate security keys, jalankan: bash scripts/generate-keys.sh
